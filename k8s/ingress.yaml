apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-gantt
  namespace: go-gantt
  labels:
    app: go-gantt
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - go-gantt.mlinarik.com
    secretName: go-gantt-tls
  rules:
  - host: go-gantt.mlinarik.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: go-gantt
            port:
              number: 8080
